{
  "name": "likhog-frontend-server",
  "version": "1.0.0",
  "description": "Express server to serve React build",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm run build:react",
    "build:react": "npm run build --prefix ."
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  }
}
